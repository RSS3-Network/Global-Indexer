apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: global-indexer
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`gi.rss3.dev`)
      services:
        - name: global-indexer
          port: 80