apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: rss3-node
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - kind: Rule
      match: Host(`gi.rss3.dev`)
      services:
        - name: global-indexer
          port: 80